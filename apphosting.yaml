# Firebase App Hosting configuration
# This file tells Firebase where to find the Next.js application

# Build configuration - uses root package.json which delegates to NextGen
build:
  commands:
    - npm install
    - npm run build

# Runtime configuration
run:
  workingDirectory: NextGen
  command: npm start

