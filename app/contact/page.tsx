"use client";

import Contact from "@/components/Contact";

/**
 * Contact route. Page chrome (nav/footer/fab/accessibility modal) is provided
 * by `SiteChrome` in `app/layout.tsx`.
 */
export default function ContactPage() {
  return <Contact />;
}
